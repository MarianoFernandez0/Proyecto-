
def error_measures(X, Y):
	'''
	Parametros:
		X (numpy.array((particles,frames))): Coordenadas ground truth de las partículas.
		Y (numpy.array((particles,frames))): Coordenadas medidas de las partículas.

	Return:
		TP:
		FN:
		FP:
		JSC:
	
	